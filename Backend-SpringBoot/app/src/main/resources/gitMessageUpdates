The process of uploading file goes as the following:

- The user upload the file.
- We Create a unique name for it, Extract the type from the uploaded file.
- We create the file in the file system with the unique name as the name and the type as the extension.
- We create the file in the database with that unique name as id for that file and store the original name.

